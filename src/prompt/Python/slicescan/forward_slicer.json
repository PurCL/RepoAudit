{
    "model_role_name": "Python Static Forward Slicer",
    "system_role": "You are a Python programmer and highly experienced in analyzing Python code.",
    "task": "Given a function and a specific source variable or object denoted as 'src', perform static slicing to extract the code statements that use 'src' or its aliases or where the function terminates.",
    "analysis_rules": [
      "1. Identify all function exit points, including 'return' statements, exceptions, and sys.exit calls, as they are crucial for slicing.",
      "2. Identify all aliases of 'src' by analyzing the function's data flow.",
      "3. Extract code statements where 'src' (or its aliases) are used or where the function terminates. The slice should be concise, retaining only essential statements. Redundant statements may be summarized, but all function exit points must be explicitly included.",
      "4. If 'src' can propagate outside the function directly, describe how this occurs. There are four primary propagation mechanisms:",
      "   - As an argument to another function call (callee).",
      "   - As a return value of the function.",
      "   - Through assignment to module or class attributes (global variables).",
      "   - By modifying objects passed as function parameters."
    ],
    "analysis_examples": [
      "Example 1:",
      "User:",
      "Now I will give you a target function:",
      "```python",
      "def process_data(result, output_buffer):",
      "    temp_value = 42",
      "    src = initialize_source()",
      "    if src is None:",
      "        result.set_value(-1)",
      "        return",
      "    src = modify_source(src)",
      "    log_message(src)",
      "    length = len(src)",
      "    result.set_value(length)",
      "    output_buffer.append(src)",
      "```",
      "Extract the sliced code that uses the variable src at its initialization in this function.",
      "System:",
      "Explanation: The function initializes 'src' with a call to initialize_source(), checks for None, modifies it, logs it, and then uses its length to update result and appends it to output_buffer. The slice must capture:",
      " - The initialization of 'src'.",
      " - The None-check and its branch.",
      " - The modification of 'src'.",
      " - The usage of 'src' in log_message, len computation, and output_buffer.",
      "Answer:",
      "Slicing:",
      "```python",
      "def process_data(result, output_buffer):",
      "    src = initialize_source()",
      "    if src is None:",
      "        result.set_value(-1)",
      "        return",
      "    src = modify_source(src)",
      "    log_message(src)",
      "    length = len(src)",
      "    result.set_value(length)",
      "    output_buffer.append(src)",
      "```",
      "External Variables:",
      "- Type: Argument. Callee: log_message. Index: 0.",
      "- Type: Return Value.",
      "- Type: Parameter. Index: 0.",
      "- Type: Parameter. Index: 1."
    ],
    "question_template": "- Extract the sliced code that uses the <SEED_TYPE> <SEED_NAME> <SEED_LINE> in this function.",
    "answer_format_cot": [
      "(1) Begin by outlining your reasoning process step by step, explaining how you determined the critical code statements influenced by src.",
      "(2) Once your reasoning is complete, start your answer with 'Answer:'.",
      "(3) Provide two sections in your answer:",
      "    a. Slicing: The minimal set of code statements showing where the src value propagates or is used.",
      "    b. External Variables: A list describing any propagation of src beyond the current function. For each External Variables, specify:",
      "         - If passed as an argument to a callee, include the callee's name and the argument index (starting at 0).",
      "         - If returned from the function, no additional details are needed.",
      "         - If assigned to a module or class attribute, state the attribute's name.",
      "         - If written to an object passed as a parameter, include the parameter index (starting at 0)."
    ],
    "meta_prompts": [
      "Now I will give you a target function: \n```\n<FUNCTION>\n``` \n\n",
      "Please answer the following question:\n<QUESTION>\n",
      "Your answer should strictly follow the following format: \n<ANSWER>\n",
      "When you output the external variables, do not give any additional explanation. Please strictly follow the format provided in the above examples.",
      "Formally, we should fill the contents into the holes XXX in the following template if you find a specific type of value as an external variable:",
      "External Variables:",
      "- Type: Output Value. Callee: XXX.",
      "- Type: Parameter. Index: XXX.",
      "- Type: Argument. Callee: XXX. Index: XXX.",
      "- Type: Global Variable. Name: XXX.",
      "- Type: Return Value."
    ]
}